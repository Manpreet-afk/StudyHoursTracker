-Setup Instructions

Follow these steps to run the Study Tracker on your local machine.

1. Prerequisites
- Install Python 3
- Install PostgreSQL

2. Database Setup
Open your standard SQL Shell (psql), log in as the default `postgres` user, and run the following command to create the database:

CREATE DATABASE studytrackerapp;
